import React, { FC, PropsWithChildren } from "react";

const SUpSection: FC<PropsWithChildren> = ({ children }) => {
  return <div style={{ background: "#BD3C3C" }}>{children}</div>;
};

export default SUpSection;
